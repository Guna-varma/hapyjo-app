/**
 * Demo notification scheduling is handled in AppNavigation (on login) and AuthContext (on logout).
 * This component exists only to satisfy the import; it renders nothing.
 */
export function DemoNotificationScheduler() {
  return null;
}
